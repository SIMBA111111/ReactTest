name: GitHub Actions Demo
run-name: ${{ github.actor }} is changed project 🚀
on: [push]
jobs:
  Clon-repo:
    runs-on: ubuntu-22.04
    steps: 
      - run: echo "Start cloning repo.."
      
      - name: Checkout code
        uses: actions/checkout@v2 
        
      - name: Install dependencies
        run: npm install
        
      - name: Build
        run: npm run build
                
      - name: Create SSH key
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/private.key
          chmod 600 ~/.ssh/private.key

      - name: Add the host to known_hosts
        run: ssh-keyscan -H 89.169.1.160 >> ~/.ssh/known_hosts

      - name: Deploy
        run: |
          ssh -i ~/.ssh/private.key root@89.169.1.160
